/**
 * Couleurs des propositions de boucles, partagées entre la carte et les
 * fiches (constante hors module client : importable côté serveur aussi).
 */
export const LOOP_COLORS = ["rgb(var(--slate))", "rgb(var(--sage))", "rgb(var(--ochre))"];
